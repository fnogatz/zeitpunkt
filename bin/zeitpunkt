#!/usr/bin/env node

var program = require('commander')

program
  .version(require('../package.json').version)
  .command('transform', 'Transform a GeoJSON stream')
  .command('simplify', 'Simplify GeoJSON')
  .command('from', 'Create GeoJSON from given input')
  .parse(process.argv)
